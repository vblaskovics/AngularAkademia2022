import { ConvertGbPipe } from './convert-gb.pipe';

describe('ConvertGbPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertGbPipe();
    expect(pipe).toBeTruthy();
  });
});
