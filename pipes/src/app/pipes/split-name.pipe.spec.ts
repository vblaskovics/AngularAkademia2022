import { SplitNamePipe } from './split-name.pipe';

describe('SplitNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitNamePipe();
    expect(pipe).toBeTruthy();
  });
});
