import { LastModifPipe } from './last-modif.pipe';

describe('LastModifPipe', () => {
  it('create an instance', () => {
    const pipe = new LastModifPipe();
    expect(pipe).toBeTruthy();
  });
});
