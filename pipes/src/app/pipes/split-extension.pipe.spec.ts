import { SplitExtensionPipe } from './split-extension.pipe';

describe('SplitExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
