import { ElapsedSecondsPipe } from './elapsed-seconds.pipe';

describe('ElapsedSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new ElapsedSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
