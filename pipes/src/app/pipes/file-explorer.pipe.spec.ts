import { FileExplorerPipe } from './file-explorer.pipe';

describe('FileExplorerPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExplorerPipe();
    expect(pipe).toBeTruthy();
  });
});
