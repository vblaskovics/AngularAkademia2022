import { FileExplorer3Pipe } from './file-explorer3.pipe';

describe('FileExplorer3Pipe', () => {
  it('create an instance', () => {
    const pipe = new FileExplorer3Pipe();
    expect(pipe).toBeTruthy();
  });
});
