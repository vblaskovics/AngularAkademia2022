import { FilenameConverterPipe } from './filename-converter.pipe';

describe('FilenameConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
