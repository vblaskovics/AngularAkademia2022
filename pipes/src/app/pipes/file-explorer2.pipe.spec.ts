import { FileExplorer2Pipe } from './file-explorer2.pipe';

describe('FileExplorer2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FileExplorer2Pipe();
    expect(pipe).toBeTruthy();
  });
});
