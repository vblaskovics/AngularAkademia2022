import { ConvertMbPipe } from './convert-mb.pipe';

describe('ConvertMbPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMbPipe();
    expect(pipe).toBeTruthy();
  });
});
