import { ColorGeneratorDirective } from './color-generator.directive';

describe('ColorGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
